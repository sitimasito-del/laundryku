{
  "version": 2,
  "installCommand": "echo Skipping dependency install for static deployment",
  "buildCommand": "echo Static deployment ready",
  "outputDirectory": "public",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
